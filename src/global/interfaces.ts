export interface HeroData {
  hairline: string;
  title: string;
  description: string;
  callToAction: string;
};

export interface BannerData {
  hairline?: string;
  title: string;
  text?: string;
  callToAction?: string;
}